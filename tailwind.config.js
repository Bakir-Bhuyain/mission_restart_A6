// tailwind.config.js (v3.x example)
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}", // Paths to your project files
  ],
  theme: {
    extend: {}, // For extending the default theme
  },
  plugins: [], // For adding plugins
};
